"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.cleanQuoteString = cleanQuoteString;

var _constants = require("../utils/constants");

/* eslint-disable no-useless-escape */
function cleanQuoteString(quote) {
  return quote // replace smart closing quotation mark with correct one
  .replace(/\”/gi, '"') // remove space before smart opening quotation mark
  .replace(/\“ /gi, '"') // replace smart opening quotation mark with correct one
  .replace(/\“/gi, '"') // add space after
  .replace(/,\"/gi, ', "') // remove space after opening quotation mark
  .replace(/, \" /gi, ', "') // remove spaces before question marks
  .replace(/\s+([?])/gi, '$1') // remove double spaces
  .replace(/ {2}/gi, ' ') // remove spaces before commas
  .replace(/ , /gi, ', ') // remove spaces before periods
  .replace(/ ."/gi, '."') // remove space before apostrophes
  .replace(/ ’./gi, '’.').trim().replace(/\.../g, _constants.ELLIPSIS).replace(/\…/g, ' … ').replace(/\ {2}… {2}/gi, ' … ');
}